import classes from './notes.module.css';

export default function Notes() {
    return(
        <div className={classes.container}> 
            <h2 className={classes.notestext}> Notes and Advertisement </h2>
        </div>
    )
}